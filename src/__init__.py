"""LEPOS core package: data loading, scoring, optimization, and helpers.

The Streamlit app in ``app/`` imports everything it needs from here so that
business logic stays independent of the UI layer.
"""
